[package]
name = "pressakey"
version = "0.1.0"
edition = "2024"

[dependencies]
argh = "0.1"

[dependencies.crossterm]
version = "0.29"
default-features = false
features = ["events"]

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
